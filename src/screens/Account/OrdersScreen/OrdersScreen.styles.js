import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    padding: 10,
  },
  noOrders: {
    textAlign: "center",
    paddingTop: 20,
    fontSize: 18,
  },
  title: {
    fontSize: 20,
  },
});
