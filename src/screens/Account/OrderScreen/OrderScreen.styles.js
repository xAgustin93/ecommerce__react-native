import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    padding: 10,
    marginBottom: 50,
  },
  title: {
    fontWeight: "bold",
    fontSize: 22,
  },
  totalPayment: {
    textAlign: "center",
    fontWeight: "bold",
    fontSize: 22,
  },
});
